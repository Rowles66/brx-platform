# macOS
.DS_Store



# Dependencies
node_modules
.pnpm-store/

# Build outputs
.next/
dist/ 
# Vercel - keep config for team, ignore cache/output
.vercel/output/
!.vercel/project.json
!.vercel/README.txt

# Environment files - with 1Password refs, these are safe to commit
# Only ignore files with actual secrets (none in this project)
.env.local.backup
.env.development.local.backup
.env.test.local.backup
.env.production.local.backup
# Logs - keep useful ones, ignore noisy monitoring
logs/ai-agent-monitor.log
logs/monitor.out
logs/monitor.err
